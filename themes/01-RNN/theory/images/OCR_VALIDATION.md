# OCR Validation Report

## Method

- Source PDF pages: `pdftoppm -png -r 300` + `tesseract -l rus+eng --psm 6`
- External and local SVG infographics: `inkscape --export-dpi=300` + `tesseract -l eng+rus --psm 11`
- OCR used as additional verification layer alongside `pdftotext` and `mutool draw -F txt`.

## Source PDF pages (key excerpts)

### page-2.png

```text
2
RNN LSTM GRU
о, h, h,
Сы С, вы Ms
h,
hy
hy h,
x х, X
[o] sigmoid function [ tanh | hyperbolic tangent function @ subtract from one
@ pointwise addition ® pointwise multiplication @ vector concatenation
Рис. 1. Сравнение архитектур: RNN запоминает информацию между шагами
Математика RNN: Пошагово
Шаг 1: Чтоу нас есть?
© т, - входной сигнал в момент времени $ (например, слово в предложении)
® h,_, - память о предыдущем шаге (что мы помним)
® h, - новая память (что мы запомним сейчас)
© Y; - выход (наш ответ)
Шаг 2: Как вычисляется новая память?
hy, = апВ (ИХ, а + Won: % + Op)
Разберем по частям:
1. И’, » - <, - “обрабатываем новую информацию”
® W,,, - матрица весов (как важна новая информация?)
® Умножение означает: “насколько сильно новая информация влияет на память?”
2. Wap: В,_1 - “вспоминаем старую информацию”
® И’, , - матрица весов (как важна старая память?)
® Умножение означает: “насколько сильно старая память влияет на новую?”
3. +6, - “добавляем смещение”
® Это как “базовый уровень активности” нейрона
4. tanh(...) - “нормализуем результат”
e Функция tanh сжимает результат в диапазон от -1 до 1
® Это предотвращает “взрыв” значений
Шаг 3: Как получается выход?
Ut = Why hy +b,
Простыми словами: Берем нашу новую память h,, умножаем на веса W), ,, и получаем ответ.
© Dmitry Vlasov 2025. All rights reserved.
Licensed under CC BY-NC-ND 4.0 — https://creativecommons.org/licenses/by-nc-nd/4.0)
```

### page-4.png

```text
и“
Архитектура LSTM: Четыре компонента
ht
ри Ен Ce
tanh
tanh
= Nd И Пони ht
Xt
Inputs: outputs: Nonlinearities: Vector operations:
Input vector Memory from Sigmoid (x) Element-wise
current block multiplication
Memory from Output of Hyperbolic Element-wise
previous block current block tangent Summation /
Concatenation

Output of
© previous block Bias: о
Рис. 2. Детальная схема ячейки LSTM с воротами
1. Cell State (Состояние ячейки) - С’
Что это? Главная “память” LSTM - информация течет через неё почти без изменений.
Аналогия: Это как конвейерная лента на заводе - информация движется по ней, а рабочие (ворота) решают, что
добавить или убрать.
2. Forget Gate (Ворота забывания) - },
Задача: Решить, какую информацию из старой памяти забыть.
Формула:

f= o(Wy [Аж + br)
Разбор по шагам:
1. №1), | - объединяем старую память и новый вход
e Если р, | = [0.2,0.П их, = [0.5, —0.3]
e To 1, 1, т,| = [0.2, 0.1, 0.5, —0.3]
```

### page-6.png

```text
И
1. Прямой путь для градиентов
В состоянии ячейки С, информация течет почти без изменений:
С — ОС, 1 +400,
Градиент по C',_, равен просто f,, без сложных умножений Ha веса.
2. Селективная память
Ворота позволяют: - Забывать ненужную информацию (forget gate) - Запоминать важную информацию (input gate)
- Контролировать выход (output gate)
СВО: Упрощенная версия
Идея GRU
GRU (Gated Recurrent Unit) - это “упрощенная LSTM” с двумя воротами вместо трех.
Основные отличия: - Нет отдельного состояния ячейки (используется только й,) - Объединены forget и input gates в
один update gate - Меньше параметров = быстрее обучение
h,
C1 i. >} С 1
- ee Ml rs "
Х,
Рис. 3. Схема ячейки СКО с двумя воротами
Компоненты СВО
1. Update Gate (Ворота обновления) - 5,
Задача: Решить, сколько старой информации сохранить и сколько новой добавить.
Формула:
2, = o(W, - [hy_1, 2] + 0.)
Интуиция: - 2, = 0 означает “забыть все старое, использовать только новое” - Z, = 1 означает “сохранить все старое,
```

### page-7.png

```text
К
игнорировать новое” - 2, = 0.3 означает “70% нового, 30% старого”
2. Reset Gate (Ворота сброса) -т,
Задача: Решить, насколько предыдущая память важна для вычисления новой информации.
Формула:
ry = OW, [hy-1, 2] + 6,)
Интуиция: - 7, = 0 означает “игнорировать предыдущую память при создании новой” - 7, = 1 означает “полностью
использовать предыдущую память”
3. Candidate Hidden State (Кандидат нового состояния) - |,
Задача: Создать “предложение” для нового состояния памяти.
Формула:
h, = tanh(W,, . [ry ©) hut; т, + b;,)
Разбор: - 7, © h,_1 - “отфильтрованная” старая память - [r, © №,_1, ‚| - объединяем отфильтрованную память с
новым входом - tanh(...) - нормализуем в диапазон [-1, 1]
4. Final Hidden State (Итоговое состояние) - hi,
Формула:
В, = (1-2) ОА ОЙй,
Разбор: - (1—z,) Oh,_, - “доля старой памяти” - 2, © hy - “доля новой информации” - Сумма дает итоговое состояние
Пример с числами: - Старое состояние: #,_| = [0.5, —0.2] - Update gate: z, = [0.3, 0.7] - Кандидат: h, = [0.8, 0.1]
Результат:
h, = [0.7 x 0.5, 0.3 x (—0.2)] + [0.3 х 0.8, 0.7 x 0.1]
h, = [0.35, —0.06] + [0.24, 0.07] = [0.59, 0.01]
Пошаговый пример работы СВО
Обрабатываем предложение: “Я изучаю нейросети”
Инициализация: - № = [0,0] (пустая память)
Шаг 1: Слово “Я” - x, = вектор для “Я” - 21 = 0 (W,  [9,51|) = [0.6, 0.4] (больше обновляем первый элемент) -
r, =0(W,.- 1,21) = [0.2, 0.8] (мало используем старую память) - h, = tanh(W,, - т, © ho, x,]) = [0.3, —0.1]
- hy = (1 — 21) © ho + 21 © hy = (0.18, —0.04]
Шаг 2: Слово “изучаю?” - 1. = вектор для “изучаю” - 2> = [0.4, 0.7] (больше обновляем второй элемент) - "> =
(0.9, 0.5] (хорошо используем память о “Я”) - hy = [0.2, 0.6] - hy = [0.6 x 0.18, 0.3 x (—0.04)] + [0.4 x 0.2, 0.7 x
0.6] = [0.188, 0.408]
И так далее...
Сравнение всех архитектур
Таблица сравнения
```

## External + generated infographics (OCR snippets)

### Gated_Recurrent_Unit_base_type.png

```text
y(t]

h[t-1]

4

wa

>

<

х

+ = |. h(t]

М

1

Л

r[t]

a

на Г Нч

О

- о

= tanh

J
```

### LSTM_cell.png

```text
hy

CLs

С!

ош.

nes

hy

о ©

Xt
```

### Recurrent_neural_network_unfold.png

```text
Unfold

© =. ®

ai ВП

Са =». >

© о о
```

### gated_cell_dataflow.png

```text
—

а зн

- iit

в ~~ — =
```

### rnn_architecture_selection_map.png

```text
>

на

и —___
```

### rnn_forward_unrolled.png

```text
```

